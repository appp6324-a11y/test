# UI ONLY — no backend
# This demo is UI-only. No env values required.
# Example variables for future integration:
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_SENTRY_DSN=
STRIPE_PUBLIC_KEY=
